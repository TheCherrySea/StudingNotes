<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sakura.dao.UserDao">
   <insert id="addUser"  useGeneratedKeys="true" parameterType="User">
        insert into t_user(name,phone,loginName,loginPwd)
        values(#{name},#{phone},#{loginName},#{loginPwd});
    </insert>
    <delete id="deleteUser" parameterType="int">
        delete from t_user where id=#{id};
    </delete>

    <update id="updateUser" parameterType="User">
        update t_user set name=#{name},phone=#{phone},loginName=#{loginName},loginPwd=#{loginPwd}
        where id=#{id};
    </update>

    <select id="queryUser" resultType="User">
select * from t_user where id=#{id};
    </select>

    <select id="queryAllUser" resultType="User">
        select * from t_user;
    </select>

    <select id="queryUserByName" resultType="User">
select * from t_user where name like concat('%',#{name},'%');
    </select>
</mapper>